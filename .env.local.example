# Copy to .env.local. The app degrades gracefully if any of these are missing:
# without keys it runs with a local conversation partner, heuristic coaching and
# the local skill-graph engine.

# Gemma - the private, local coaching agent. Point at a local model server and the
# learner's transcript is analysed on your machine, never sent to a hosted model.
# Both protocols are auto-detected:
#   Ollama:    ollama pull gemma3:4b            ->  GEMMA_URL=http://localhost:11434
#   LM Studio: load a Gemma model, start server ->  GEMMA_URL=http://localhost:1234
# Without it, coaching falls back to Sarvam (if configured), then to built-in heuristics.
GEMMA_URL=
# GEMMA_MODEL=gemma3:4b   # optional hint; the closest Gemma model the server lists is used

# Gemini - the live conversation partner (natural dialogue, follow-ups, pushback).
GEMINI_API_KEY=
# GEMINI_MODEL=gemini-2.5-flash        # optional override; unavailable models fall back
#                                      # through gemini-2.5-flash -> 2.0-flash -> 1.5-flash

# Sarvam AI - natural voice for Indian users: Saarika STT (multilingual, code-mixing),
# Bulbul TTS, and the chat model used for coaching summaries.
SARVAM_API_KEY=

# Optional Sarvam overrides - leave commented to use the defaults shown:
# SARVAM_CHAT_MODEL=sarvam-105b        # hosted LLM id: sarvam-105b or sarvam-30b
# SARVAM_REASONING_EFFORT=none         # none | low | medium | high ("none" required on starter tier)
# SARVAM_MAX_TOKENS=4096               # per-request max_tokens ceiling (starter-tier cap)
# SARVAM_TTS_MODEL=bulbul:v2           # Bulbul model for read-aloud
# SARVAM_TTS_SPEAKER=anushka           # Bulbul voice

# Cognee - the semantic layer of the Skill Knowledge Graph. When set, every completed
# practice session is mirrored into a per-learner Cognee dataset (add + cognify) and
# recall() is enriched with Cognee's graph-grounded "what should I practice next?".
# When unset, the deterministic local graph engine provides the same
# remember/recall/replay/forget semantics with zero credentials.
COGNEE_API_URL=
COGNEE_API_KEY=
# COGNEE_DATASET=skill-graph                 # optional dataset name prefix
# SKILL_GRAPH_DATA_DIR=./.skill-memory       # local graph store (use /tmp/skill-memory on serverless)

# DEMO_MODE=true substitutes clearly-labelled sample data when a service is unavailable
# (e.g. a demo transcript if STT fails). Never enable in production.
DEMO_MODE=
